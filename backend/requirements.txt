# Minimal, ultra-compatible versions to avoid all typing issues
fastapi==0.88.0
uvicorn==0.20.0
python-multipart==0.0.5

# Google AI - using older stable version
google-generativeai==0.3.2

# PDF Processing - stable version
PyPDF2==3.0.1

# Environment
python-dotenv==1.0.0

# Use basic pydantic that definitely works
pydantic==1.9.2

# HTTP client - older version
httpx==0.23.3

# Force older typing-extensions to avoid ForwardRef issues
typing-extensions==4.4.0

# Pin starlette to match fastapi version exactly
starlette==0.22.0