# FastAPI and Server - Using older stable versions that don't require Rust
fastapi==0.95.2
uvicorn==0.22.0
python-multipart==0.0.6

# Google AI Integration - Using older version that doesn't require Rust dependencies
google-generativeai==0.3.0

# PDF Processing
PyPDF2==3.0.1

# Environment Variables
python-dotenv==1.0.0

# Data validation - Using Pydantic V1 (final version) - NO RUST COMPILATION
pydantic==1.10.17

# HTTP client - Older version without Rust dependencies
httpx==0.24.0

# Core compatibility - Pinning to avoid Rust compilation
starlette==0.27.0
typing-extensions==4.5.0